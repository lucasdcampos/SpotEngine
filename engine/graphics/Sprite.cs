namespace SpotEngine
{
    /// <summary>
    /// Sprite is a 2D texture that has a base color
    ///and an associated image.If the image is null, then
    ///a simple square will be rendered.
    /// </summary>
    public class Sprite
    {
        public Color color;
        public string texturePath = "";
    }
}
